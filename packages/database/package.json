{
	"name": "database",
	"version": "1.0.0",
	"module": "src/index.js",
	"type": "module",
	"private": true,
	"scripts": {
		"env:load": "varlock load",
		"db:migrate": "varlock run --no-redact-stdout -- drizzle-kit migrate",
		"db:push": "varlock run --no-redact-stdout -- drizzle-kit push",
		"db:generate": "varlock run --no-redact-stdout -- drizzle-kit generate",
		"db:studio": "varlock run --no-redact-stdout -- drizzle-kit studio"
	},
	"exports": {
		".": "./src/index.ts"
	},
	"dependencies": {
		"drizzle-orm": "^1.0.0-rc.4-fb12281",
		"varlock": "^1.16.1"
	},
	"devDependencies": {
		"@types/bun": "catalog:",
		"drizzle-kit": "^1.0.0-rc.4",
		"typescript": "catalog:"
	}
}
